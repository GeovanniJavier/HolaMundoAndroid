package unitec.jc.org.ui.home

import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import androidx.lifecycle.ViewModel

class HomeViewModel : ViewModel() {
    /* el siguiente metodo contiene datos que pueden cambiar asincronicamente
    // por ejemplo un list de usuario
    private val _text = MutableLiveData<String>().apply {
        value = "Aqui se va a visualizar la información del back end invocada con un metodo"
        //Aquí puede invocarse un metodo para cargar los usuarios desde
        //un metodfo asinronico DESDE EL BACK END
    }
    val text: LiveData<String> = _text*/
}